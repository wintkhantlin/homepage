<footer class="relative z-10 px-4 pb-12 pt-6 sm:px-6 lg:px-8">
  <div class="mx-auto w-full max-w-6xl">
    <div class="footer-board relative overflow-hidden px-6 py-10 sm:px-10 sm:py-12">
      <div class="tape tape-a" />
      <div class="tape tape-b" />

      <div class="relative z-10 grid gap-10 md:grid-cols-[1.25fr_0.75fr] md:gap-12">
        <div class="space-y-6">
          <p class="text-xs font-bold uppercase tracking-[0.24em] text-(--text-faint)">
            Building In Public
          </p>

          <h2 class="max-w-2xl text-4xl font-black leading-[1.03] tracking-[-0.03em] text-(--text-heading) sm:text-5xl md:text-6xl">
            I’m focused on <span class="text-sky-500">learning</span>, shipping
            thoughtful experiments, and documenting the process.
          </h2>

          <div class="flex flex-wrap gap-3">
            <a href="mailto:wintkhantlin@gmail.com" class="footer-button">Say hello</a>
            <a href="/#projects" class="footer-button">View projects</a>
          </div>
        </div>

        <div class="space-y-5">
          <h3 class="text-xs font-bold uppercase tracking-[0.25em] text-(--text-faint)">
            Elsewhere
          </h3>

          <ul class="space-y-3">
            <li>
              <a
                href="https://github.com/wintkhantlin"
                target="_blank"
                rel="noreferrer"
                class="footer-link font-mono"
              >
                GitHub
              </a>
            </li>
            <li>
              <a
                href="https://www.linkedin.com/in/wintkhantlin/"
                target="_blank"
                rel="noreferrer"
                class="footer-link font-mono"
              >
                LinkedIn
              </a>
            </li>
            <li>
              <a href="mailto:wintkhantlin@gmail.com" class="footer-link font-mono">
                Email
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer-board {
    border: 1px solid var(--border);
    border-radius: 24px;
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--bg) 94%, white 6%), color-mix(in srgb, var(--bg) 98%, black 2%));
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.18);
  }

  .footer-board::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.05;
    background-image:
      repeating-linear-gradient(
        -8deg,
        rgba(255, 255, 255, 0.06) 0,
        rgba(255, 255, 255, 0.06) 1px,
        transparent 1px,
        transparent 10px
      );
  }

  .tape {
    position: absolute;
    width: 90px;
    height: 22px;
    background: color-mix(in srgb, #facc15 62%, white 38%);
    border: 1px solid rgba(255, 255, 255, 0.12);
    transform: rotate(-8deg);
    opacity: 0.42;
    z-index: 2;
  }

  .tape-a {
    top: 14px;
    right: 32px;
    transform: rotate(7deg);
  }

  .tape-b {
    bottom: 16px;
    left: 40px;
  }

  .footer-link {
    display: inline-flex;
    align-items: center;
    border-bottom: 2px solid transparent;
    font-size: 0.92rem;
    font-weight: 500;
    color: var(--text-muted);
    transition: transform 130ms ease, border-color 130ms ease, color 130ms ease;
  }

  .footer-link:hover {
    transform: translateX(5px);
    border-bottom-color: var(--text-heading);
    color: var(--text-heading);
  }

  .footer-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.7rem 1rem;
    color: var(--text-heading);
    text-decoration: none;
    transition: transform 130ms ease, border-color 130ms ease, background-color 130ms ease;
  }

  .footer-button:hover {
    transform: translateY(-1px);
    border-color: color-mix(in srgb, var(--text-heading) 55%, var(--border) 45%);
    background: color-mix(in srgb, var(--text-heading) 8%, transparent 92%);
  }

  @media (max-width: 768px) {
    .tape {
      display: none;
    }
  }
</style>
