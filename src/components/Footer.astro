<footer class="relative z-10 px-4 pb-12 pt-6 sm:px-6 lg:px-8">
  <div class="mx-auto w-full max-w-6xl">
    <div class="footer-board relative overflow-hidden px-6 py-10 sm:px-10 sm:py-12">
      <div class="tape tape-a" />
      <div class="tape tape-b" />

      <div class="relative z-10 grid gap-10 md:grid-cols-[1.25fr_0.75fr] md:gap-12">
        <div class="space-y-6">
          <p class="heading-sans text-xs font-bold uppercase tracking-[0.24em] text-black/58">
            Building In Public
          </p>

          <h2 class="heading-sans max-w-2xl text-4xl font-black leading-[1.03] sm:text-5xl md:text-6xl">
            I’m focused on <span class="highlight-blue">learning</span>, shipping
            thoughtful experiments, and documenting the process.
          </h2>
          
          <div class="flex flex-wrap gap-3">
            <a href="mailto:wintkhantlin@gmail.com" class="btn-doodle">Say hello</a>
            <a href="/#projects" class="btn-doodle">View projects</a>
          </div>
        </div>

        <div class="space-y-5">
          <h3 class="heading-sans text-xs font-bold uppercase tracking-[0.25em] text-black/60">
            Elsewhere
          </h3>

          <ul class="space-y-3">
            <li>
              <a
                href="https://github.com/wintkhantlin"
                target="_blank"
                rel="noreferrer"
                class="footer-link body-mono"
              >
                GitHub
              </a>
            </li>
            <li>
              <a
                href="https://www.linkedin.com/in/wintkhantlin/"
                target="_blank"
                rel="noreferrer"
                class="footer-link body-mono"
              >
                LinkedIn
              </a>
            </li>
            <li>
              <a href="mailto:wintkhantlin@gmail.com" class="footer-link body-mono">
                Email
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer-board {
    border: 1.5px solid rgba(24, 24, 24, 0.58);
    border-radius: 26px 16px 30px 14px / 15px 28px 16px 24px;
    background: color-mix(in srgb, var(--paper-cream) 90%, white 10%);
    box-shadow: 3px 4px 0 rgba(24, 24, 24, 0.25);
  }

  .footer-board::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.05;
    background-image:
      repeating-linear-gradient(
        -8deg,
        rgba(24, 24, 24, 0.08) 0,
        rgba(24, 24, 24, 0.08) 1px,
        transparent 1px,
        transparent 10px
      );
  }

  .tape {
    position: absolute;
    width: 90px;
    height: 22px;
    background: color-mix(in srgb, var(--highlighter-yellow) 58%, white 42%);
    border: 1px solid rgba(24, 24, 24, 0.2);
    transform: rotate(-8deg);
    opacity: 0.42;
    z-index: 2;
  }

  .tape-a {
    top: 14px;
    right: 32px;
    transform: rotate(7deg);
  }

  .tape-b {
    bottom: 16px;
    left: 40px;
  }

  .footer-link {
    display: inline-flex;
    align-items: center;
    border-bottom: 2px solid transparent;
    font-size: 0.92rem;
    font-weight: 500;
    color: rgba(24, 24, 24, 0.72);
    transition: transform 130ms ease, border-color 130ms ease, color 130ms ease;
  }

  .footer-link:hover {
    transform: translateX(5px);
    border-bottom-color: rgba(24, 24, 24, 0.65);
    color: rgba(24, 24, 24, 1);
  }

  @media (max-width: 768px) {
    .tape {
      display: none;
    }
  }
</style>
