2026-04-27: Updated the homepage header to use a floating white pill nav with a strong wordmark, centered links, a plain secondary action, and a dark CTA. Keep nav-only visual updates isolated to `src/components/Header.astro` and shared styling in `src/styles/global.css` unless the page layout itself needs to change.
2026-04-27: Added homepage polish patterns: sticky nav state with section-aware active links, reveal-on-scroll hooks via `data-reveal`, filterable project rows with status chips, neutral writing cards with read-time/language cues, and a dedicated `/writing` archive page.
2026-04-30: SEO schema helpers now live in `src/lib/seo.ts` (`absoluteUrl`, `createPersonSchema`, `createWebsiteSchema`, `createBreadcrumbSchema`). Page routes should pass canonical paths and structured data through `BaseLayout`; avoid a static `public/robots.txt` because `src/pages/robots.txt.ts` generates the sitemap-aware version.
2026-04-30: Dependency cleanup removed the unused Unlighthouse audit stack (`@unlighthouse/cli`, `puppeteer`, `serve`, `wait-on`) and its CI job, plus unused `@astrojs/check` and `@iconify-json/heroicons`. Keep `@iconify-json/lucide`, Tailwind typography, Husky, and active Astro integrations because current components/configs use them.
2026-08-27: When applying the `gpt-taste` direction to this homepage, preserve its minimalist Astro structure unless a full redesign is explicitly requested. Favor small editorial refinements: wider content, two-line hero copy, larger chapter spacing, restrained hover movement, and no added client framework or motion dependency.
2026-08-27: The homepage introduction now uses a Daniel Sun-inspired UX pattern adapted to Wint's identity: full-viewport framed hero, low-anchored editorial serif statement, CSS-only technical grid and theme-aware chartreuse glow, plus one primary work CTA. Keep mobile hero copy shorter than desktop to preserve the first-view hierarchy.
2026-08-27: Homepage consistency pass removed the hero grid and frame, standardized major sections on `py-32`, aligned project and writing row interaction timing, improved muted-text contrast, removed the duplicate About contact card, and added Instrument Serif through Astro's font pipeline for the hero display face.
2026-08-27: Project screenshots are consistently near 16:9: GenCourse 2048×1165, URL2Short 1280×726, and Zay Chin/Chate Sat/ToolyDooly 1440×806. Render them with Astro `<Image>` inside `aspect-video object-contain` frames. Project detail pages are generated at `/project/[slug]` from typed `caseStudy` YAML fields; keep problem, approach, architecture, and highlights grounded in each repository's documentation.
2026-08-27: Editorial headings now use the self-hosted OFL-licensed LT Superior Serif regular face at `src/assets/fonts/LTSuperiorSerif-Regular.otf`, configured through Astro's local font provider as `--font-display`. Keep the bundled `LTSuperiorSerif-OFL.txt` license beside the font.
2026-08-27: Project detail routes guard `project.data.caseStudy` at render time even though the content schema requires it. This prevents stale Astro development content-cache entries from crashing after schema changes; incomplete entries render a project overview until the cache refreshes.
2026-08-27: Project case studies are English-only. Keep their content schema flat (`status`, `challenge`, `approach`, `architecture`, `highlights`, and `diagram`) and keep every Mermaid flowchart label in English; do not reintroduce a case-study language switch.
2026-08-27: The footer navigation is a macOS-inspired dock in `src/components/Footer.astro`: keep it embedded below the contact CTA, preserve the glass shelf, app-like Lucide tiles, active-page dot, and pointer-distance magnification. Touch and reduced-motion users intentionally get a static, horizontally scrollable dock.
2026-08-27: SEO metadata supports optional `seoTitle` and `seoDescription` fields for both posts and projects. Keep user-facing titles/descriptions separate from search snippets, target roughly 50–60 characters for titles and 120–158 for descriptions, and preserve the trailing-slash canonical normalization in `src/lib/seo.ts` so canonicals match the generated sitemap.
2026-08-27: Homepage identity metadata uses the optimized `happer.png` portrait for Person schema and social previews; do not revert the Person image to the favicon because creator images should represent the person. `BaseLayout` also provides RSS discovery, Open Graph image alt text, profile metadata, and a compact Twitter card fallback for the square default icon.
2026-08-28: The About section technology stack uses locally installed `@iconify-json/devicon` assets through `astro-icon`. Keep the labelled, static two-column list on mobile; use the duplicated CSS marquee only from the `sm` breakpoint upward, with desktop hover pause and a static wrapped fallback under `prefers-reduced-motion`.
2026-08-28: Homepage competition results live in `src/components/home/RecognitionSection.astro` between Selected Work and Writing. Keep the zero-JavaScript editorial ledger, reverse-chronological order, semantic month dates, and restrained text treatment instead of medal cards or emoji.
2026-08-28: Use `devicon:nodejs-wordmark` for the About technology strip. The plain `devicon:nodejs` asset is gradient-only and can render blank when `astro-icon` reuses it through an SVG symbol.
2026-08-28: Use `devicon:python-wordmark` for the About technology strip for the same SVG-symbol compatibility reason; the plain `devicon:python` asset is gradient-only. The remaining configured Devicon assets are not gradient-only.
2026-08-28: Recognition imagery lives in `src/assets/img/awards/`. Keep `RecognitionSection.astro` as a compact award ledger: small silver/gold placement medallions, concise editorial rows, and proportion-aware evidence images. GUSTO's three-image strip follows the source ratios (square, 800×1422 portrait, and 3:4); Let's Tech stays a small 3:4 portrait; the photo-less Technical Competition uses a compact CSS seal. Set deliberate Astro `<Image>` output widths instead of serving near-source-size assets.
